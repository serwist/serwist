{
  "name": "@serwist-tests/build",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run --config ../vitest.config.ts"
  },
  "devDependencies": {
    "@serwist/build": "workspace:*",
    "serwist": "workspace:*",
    "vitest": "2.1.8"
  }
}
