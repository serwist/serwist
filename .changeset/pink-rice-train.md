---
"@serwist/cli": patch
---

feat(cli): added build command

- This command uses esbuild to build the service worker. You must install `esbuild` before running this command.

- To use this command, run `serwist build` or `serwist build --watch`.